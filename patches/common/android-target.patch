diff --git a/packages/opencode/script/build.ts b/packages/opencode/script/build.ts
index ce45f77..5276428 100755
--- a/packages/opencode/script/build.ts
+++ b/packages/opencode/script/build.ts
@@ -55,6 +55,7 @@ const allTargets: {
   arch: "arm64" | "x64"
   abi?: "musl"
   avx2?: false
+  android?: boolean
 }[] = [
   {
     os: "linux",
@@ -85,6 +86,11 @@ const allTargets: {
     abi: "musl",
     avx2: false,
   },
+  {
+    os: "linux",
+    arch: "arm64",
+    android: true,
+  },
   {
     os: "darwin",
     arch: "arm64",
@@ -150,6 +156,7 @@ for (const item of targets) {
     item.arch,
     item.avx2 === false ? "baseline" : undefined,
     item.abi === undefined ? undefined : item.abi,
+    item.android ? "android" : undefined,
   ]
     .filter(Boolean)
     .join("-")
@@ -160,6 +167,10 @@ for (const item of targets) {
   const treeSitterWorkerPath = "opentui-tree-sitter-worker.js"
   const bunfsRoot = item.os === "win32" ? "B:/~BUN/root/" : "/$bunfs/root/"
 
+  const compileTarget = item.android
+    ? `bun-${item.os}-${item.arch}-android`
+    : name.replace(pkg.name, "bun")
+
   await Bun.build({
     conditions: ["bun", "node"],
     tsconfig: "./tsconfig.json",
@@ -174,7 +185,7 @@ for (const item of targets) {
       autoloadDotenv: false,
       autoloadTsconfig: true,
       autoloadPackageJson: true,
-      target: name.replace(pkg.name, "bun") as any,
+      target: compileTarget as any,
       outfile: `dist/${name}/bin/opencode`,
       execArgv: [`--user-agent=opencode/${Script.version}`, "--use-system-ca", "--"],
       windows: {},
@@ -196,13 +207,14 @@ for (const item of targets) {
       OTUI_TREE_SITTER_WORKER_PATH: bunfsRoot + treeSitterWorkerPath,
       OPENCODE_WORKER_PATH: workerPath,
       OPENCODE_CHANNEL: `'${Script.channel}'`,
-      OPENCODE_LIBC: item.os === "linux" ? `'${item.abi ?? "glibc"}'` : "",
-      ...(item.os === "linux" ? { "process.env.OPENTUI_LIBC": JSON.stringify(item.abi ?? "glibc") } : {}),
+      OPENCODE_LIBC: item.os === "linux" && !item.android ? `'${item.abi ?? "glibc"}'` : item.android ? `'bionic'` : "",
+      ...(item.os === "linux" && !item.android ? { "process.env.OPENTUI_LIBC": JSON.stringify(item.abi ?? "glibc") } : {}),
+      ...(item.android ? { "process.env.OPENTUI_LIBC": JSON.stringify("bionic") } : {}),
     },
   })
 
   // Smoke test: only run if binary is for current platform
-  if (item.os === process.platform && item.arch === process.arch && !item.abi) {
+  if (item.os === process.platform && item.arch === process.arch && !item.abi && !item.android) {
     const binaryPath = `dist/${name}/bin/opencode`
     console.log(`Running smoke test: ${binaryPath} --version`)
     try {
@@ -224,6 +236,7 @@ for (const item of targets) {
         os: [item.os],
         cpu: [item.arch],
         ...(item.abi ? { libc: [item.abi] } : {}),
+        ...(item.android ? { libc: ["bionic"] } : {}),
       },
       null,
       2,
